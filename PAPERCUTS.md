## 2026-08-10 21:34 — GPT-5.6

Researching the product-workflow recipes against React Hook Form's official wizard guide → the documentation endpoint returned HTTP 403 to the web reader. The same material may need to be read from the official repository source or treated as an unavailable source.

## 2026-08-10 21:40 — GPT-5.6

Applying the project's required creator-vibe lens to API research → `AGENTS.md` says the `creator-vibe` skill is installed, but it is absent from both the advertised skill catalog and the local skill roots. Install or register the skill, or update the instruction to contain the complete fallback behavior.

## 2026-08-10 22:27 — GPT-5.6

Adding the submitter snapshot helper → the first surgical patch used a stale guess for the adjacent `createDomId` implementation and did not apply. Read the exact local context before composing even a nearby insertion patch.

## 2026-08-10 22:28 — GPT-5.6

Installing the docs-only React Router dependency for the typechecked guard recipe → npm completed the install but warned about six transitive install scripts that are not covered by `allowScripts`. Review the repository's npm script policy if those packages later need their install steps.

## 2026-08-10 22:39 — GPT-5.6

Building the new physical Twoslash snippets → Vocs rejected six `^?` quick-info markers because formatting placed them after multi-line expressions in Twoslash removal ranges. Keep query markers on simple single-line expressions, or rely on the `twoslash` fence to validate full physical snippets.

## 2026-08-10 22:40 — GPT-5.6

Rebuilding the TSX Twoslash snippets after fixing query markers → Vocs used the classic JSX transform instead of the repository's `react-jsx` setting and reported TS2686 for every JSX element. Add `// @jsx: react-jsx` to physical TSX snippets that are rendered through isolated Twoslash blocks.

## 2026-08-10 22:42 — GPT-5.6

Running the docs browser suite after a manual static build → the build used the production `baseUrl`, so preview links targeted GitHub Pages and client demos did not hydrate locally. Build preview assets with both `BASE_URL=http://127.0.0.1:4175` and `BASE_PATH=/form-please` before `site:test:e2e`.

## 2026-08-10 22:53 — GPT-5.6

Encoding the final workflow caveats in a content test → an exact phrase assertion ignored Markdown backticks around `when` and failed despite the intended text being present. Assert the stable semantic fragment when documentation punctuation is not part of the contract.
